doveadm_api_key = %{env:MDA_DOVEADM_KEY}
doveadm_allowed_commands =
service doveadm {
   unix_listener doveadm-server {
      user = dovecot
   }
   inet_listener http {
        port = 12346
        ssl = yes
    }
}